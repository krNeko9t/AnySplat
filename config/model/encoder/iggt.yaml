name: iggt

instance_feat_dim: 8
pretrained_weights: ""
intermediate_layer_idx: [4, 11, 17, 23]
pred_pose: true

# Physics scheme: null | class | property | physgm_copy
phys_scheme: null
phys_feat_dim: 32
phys_ignore_id: 0
phys_use_point_feat: true
phys_use_window_cross_attn: true
# scheme == class
phys_num_classes: 4
phys_classifier_hidden: 64
phys_dense_logits: false
# phys_class_names: [static, rigid, soft, unknown]  # optional; else taken from PhysicsTarget
# scheme == property
phys_property_hidden: 64
# scheme == physgm_copy
physgm_hidden: 64
