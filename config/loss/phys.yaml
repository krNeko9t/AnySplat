# Physics loss: formula only (no classifier weights).
# Pred/target come from depth_dict["physics_prediction"] / ["physics_target"].
phys:
  weight: 1.0
  num_classes: 4
  ignore_id: 0
  focal_gamma: 2.0
  dense_aux_weight: 0.0
