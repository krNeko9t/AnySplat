---
id: T10
title: 全量训练与结果判读
type: wayfinder:task
status: open
assignee: -
blocked-by: [T9, T8]
---

## Question

**HITL：需要人在集群上跑。这是地图的终点票。**

跑完 10k steps 的全量训练，然后判定「训练问题到底修好了没有」。

验收（三层，全部要过）：

1. **训练在动**（T5 的看板）：`ins_intra` 显著下降、`ins_inter_min` 显著上升、
   `ins_cross` 下降并收敛。这一层如果不过，后面两层不用看。
2. **重建不退化**：PSNR / SSIM / LPIPS 不低于 T8 的零样本基线。
   参照论文 Table 1，InstanceSplat 相对 AnySplat 是**略有提升**的
   （20.76→20.99 @2views、21.14→21.54 @8views），所以「持平」就算过，「明显掉」就是没过。
3. **实例质量可用**（T6 的里程碑门）：HDBSCAN 解码后 mIoU 与 Acc@0.25 达到可用水平，
   PCA 可视化里同一物体是连贯的一块、相邻物体分得开（对照论文 Figure 5、Figure 6）。

**如果不过**，按这个顺序归因，一次只动一个变量：

1. 先看是不是 scaling 的锅 —— 我们偏离了论文的 batch/lr/steps（sqrt scaling）。
   退回 2 卡 × 论文原味配方（batch 8、lr 1e-4、20k steps）跑一次对照。
2. 再看 margin（R1 的取值是推断出来的，论文没给）。
3. 再看 `voxel_size`（地图 Notes 的「Not yet specified」里那条：0.002 是 AnySplat 默认，
   论文没给，体素太粗会糊掉小物体的实例边界）。
4. 最后才怀疑公式实现——但前面每张票都有 CPU 判定性测试，这里应该已经排除了。

**别做的事**：不要在这一步开始堆新 trick。地图的 Out of scope 里那些东西
（语义分支、η 硬负样本、infinigen 数据）都不是用来救火的。

**产出**：三层验收各自的结论 + 权重落盘路径 + 如果没过，指向下一张票的具体假设。
