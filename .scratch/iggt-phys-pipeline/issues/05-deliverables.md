# 05 — 交付物：bench 的叠加图 + 实例物性表，garden 不崩

Type: task
Status: closed
Blocked by: [02](02-train-physics-head.md) ✅ closed, [03](03-iggt-baseline-and-hdbscan-cost.md), [04](04-trace-two-feature-sources.md) ✅ closed
Blocks: [06 — 量成员集合这处不等价](06-quantify-the-two-mismatches.md)
Assignee: subagent (2026-09-09)

> 收口票。地图 Destination 的四条硬条件在这里被兑现或被判没兑现。

## Question

把 02（训好的物性头）、03（官方 IGGT 的实例与对照图）、04（同一份高斯上的两路特征）
合成导师后天要看的东西。

### 交付清单

1. **bench 的全分辨率实例叠加图**（原图 + 半透明实例色）。
   ⚠️ **不是**灰底 3DGS 渲染——老图 04 就是这么被判不合格的。
2. **实例物性表**：每个实例一行，`instance_id / 高斯数 / (E, ν, ρ) 的 SI 值 / var`。
   ⚠️ 老图 04 定的：`var` 与成员间 `mu_spread` **分开摆**，别混成一个数。
3. **并排对照**：同一场景、同一渲染方式下，官方 IGGT 基线（03 号票产出）与本图结果并排。
   Destination 的判据就是这一栏——**"不明显更差"由人看，不设 IoU 阈值。**
4. **新视角一致性的证据**：同一物体换视角同色。（这一条灰底渲染反而是合适的载体。）
5. **garden 上同一套脚本跑通**，只证不崩，不做验收图。
6. **一份可复现脚本**：一条命令从场景跑到上面这些产物。

### 要定的

- 输出目录跟 `trace_output/<scene>_<tag>/` 的现有约定走，还是另起。
- 落不落盘 `gau_phys_feat [P,32]`（P≈10⁶ ⇒ fp16 也有 64 MB/场景）。
  留着能不重跑 trace 就换阈值/换头；不留则每次改都要重跑 3 趟。

## 完成判据

导师那四条硬条件逐条对上：① 输入是已建好的 3DGS 场景 + 相机 ✓；② 一次前馈 ✓；
③ 链路里有一次我们完成的训练（= 02 的物性头）✓；④ 输出 3D 实例 + 每实例物性 ✓。
**任何一条没兑现，如实写进 Answer，别用图糊过去。**

---

## 02 / 04 交下来的、本票必须处理的三件事（2026-09-09）

1. ⚠️ **分辨率张力，04 标出未决，本票要拍板。**
   04 跑在 **504×336**（`iggt` 默认），为的是实例流与 03 的验收基线逐位可比；
   但物性头是在 **252×448、4 固定视角**上训的。
   `--iggt_phys_image_size 448,252` 可切换。
   **做法**：两个分辨率各解一次 (E,ν,ρ)，把差摆出来，再决定发哪个。
   实例分割的质量是 Destination 的判据（「并排不明显更差」），物性只要「有个值」
   ⇒ **默认倾向保 504×336**（不动实例流），除非物性值差得离谱。**理由要写进 Answer。**

2. ⚠️ **实例物性表要带 `n_gau` 列，并把高斯数少的实例显式标出来。**
   04 实测：小/薄实例（534–1306 高斯）的 2D↔3D 池化 cos 塌到 0.39–0.82，
   而 >1000 高斯的实例是 0.95–0.99。**不许让这些行在表里冒充等价可信。**

3. **池化落地就是「把该实例的高斯的 `gau_sem` 加起来」再过 MLP**；
   分母是什么因 LayerNorm 尺度不变而无所谓（见 01 号票的更正块）。
   `mu_spread` = 逐高斯单独 decode 出的 `mu` 的标准差，与头输出的 `var` **分两列**。

---

## Answer（2026-09-09 05:40 关闭）——导师那四条**全部兑现**，带两条诚实附注

### 交付物

**可复现脚本（一条命令，场景 → 全部产物）**
`scripts/run_iggt_phys_pipeline.py`（+ `scripts/make_ticket05_figures.py` 只拼板，不跑模型）
bench 端到端 **72.2 s**（trace 37.1 s 含模型加载与**一次** 5.76 s 的 IGGT 前馈；
KNN 7.2 s；HDBSCAN 9.4 s；物性解码；叠加）。

**图**（仓库内 JPEG）
- `.scratch/iggt-phys-pipeline/figures/05_instance_physics_panel.jpg` ← **主图**（交付物 1+2）
- `05_sidebyside_baseline_vs_ours.jpg`（交付物 3）
- `05_bench_overlay_contact_sheet.jpg`（6 视角，原生 1008×756）
- `05_bench_novelview_12views.jpg`（交付物 4，**12** 视角）
- `05_garden_partial_overlay.jpg`、`05_bicycle_overlay.jpg`

**表**：`/mnt/storage_pool/liaoyuanjun/runs/ticket05/bench_504x336/instance_physics.{md,csv,json}`

### 实例物性表（bench，504×336，step-10000 头）

| id | 实例 | n_gau | trust | ρ (kg/m³) | E (Pa) | ν |
|---|---|---|---|---|---|---|
| 1 | 猫摆件 | 27,402 | ok | 122.8 | 2.549e+06 | 0.4466 |
| 2 | 蛋挞 | 6,847 | ok | 1616.9 | 1.443e+10 | 0.2889 |
| 3 | 娃娃 | 18,876 | ok | 1006.4 | 1.836e+09 | 0.3274 |
| 4 | 玩具车 | 13,434 | ok | 1181.6 | 1.323e+10 | 0.2923 |
| 5 | **碎片（不是物体）** | **1,025** | **LOW** | 1895.3 | 1.482e+10 | 0.2766 |
| 6 | 葡萄 | 7,581 | ok | 845.7 | 3.456e+09 | 0.3136 |
| 7 | 葡萄叶 | 7,840 | ok | 2193.8 | 1.702e+10 | 0.2690 |
| 8 | 墙 **[stuff]** | 555,810 | ok | 1806.2 | 2.416e+10 | 0.2587 |
| 9 | 甲板地面 **[stuff]** | 366,893 | ok | 1889.4 | 2.491e+10 | 0.2726 |

`var` 与 `mu_spread` 各三列（ρ,E,ν），**分开摆、都在 z-score 空间、从不合并、从不进 SI**——
完整数值见 `instance_physics.md` 与主图。
主图上直接印了两条限定：`trust=LOW` 的含义，以及
**「E 不精确到优于约 1.5 倍」**（06 号票的成员集合敏感度）。

⚠️ **猫这一行不要当成绩卖**：它在两个分辨率下都是头**最不自信**的一行
（var_ρ / var_E 与 mu_spread 都最高），122.8 kg/m³ / 2.5 MPa 对一个陶瓷摆件是「软物体」的答案，
而且它正是换分辨率后动了 12× 的那一行。

### 三个待决，全部拍板

**(a) 分辨率 → 保 504×336。** 在**同一套成员集合**上两个分辨率各解一次
（`figures/resolution_comparison.md`）：ρ 中位 1.13×（0.94–2.93×），
**E 中位 0.99×（0.54–12.44×）**，ν 中位 |Δ| 0.0077。控制组（同命令两遍）三列 SI 全为 **0.000e+00**
⇒ 移动是真的。三条理由，且它**确实重新考虑过**：
1. 分割才是被判的那条，而 504×336 **逐位复现**官方 IGGT 基线（见下）。
   448×252 下标签实质改变：仍 9 簇，但墙/地板那一刀移了约 42k 高斯
   （597,869/322,330 vs 555,810/366,893），1,025 的碎片消失、新出一个 5,469 的簇。
2. 「一次前馈」是硬条件，两个分辨率不能兼得。
3. ⚠️ **唯一能说明哪个分辨率更 in-distribution 的证据指向与先验相反的方向**：
   在它自己的训练分辨率 448×252 上，头对本场景**更不自信**
   （平均 var 0.909/0.776/1.217 vs 0.856/0.688/1.059）。
   「越接近训练分辨率物性越好」在这里**没有证据支持**。
   本图不承诺物性的量化声明，两边都给得出「一个值」⇒ **由分割定夺。**

**(b) 输出目录 → 新开 `/mnt/storage_pool/liaoyuanjun/runs/ticket05/<scene>_<tag>/`**，
不走仓库相对的 `trace_output/`：根盘 96% 满，光 trace 的 `.pt` 就 208 MB/场景。
目录内部沿用旧约定（`trace/`、`labels.npz`、`instance_physics.*`、`overlay/`）。

**(c) 落盘 `gau_phys_feat` → 落，默认开。** `gau_phys_feat_fp16.pt`（79 MB，含 `num_ray` 与
`gaussian_index`）写在表旁边，`--no_keep_phys_feat` 可关。换头或换阈值现在是秒级重解码，
不用重跑 3 趟 trace。

### 导师的四条硬条件，逐条

**① 输入是已建好的 3DGS 场景 + 相机 —— 成立。**
bench 的 `point_cloud.ply`（1,045,236 高斯、2D surfel）+ COLMAP `sparse/` + 36 张照片。
没有任何重建，高斯是从盘上读的。

**② 一次前馈 —— bench 上成立，且是被显式做成这样的。**
`IGGT+phys: 1/1 [00:05<00:00, 5.76s/it]`，36 视角一次前馈。
⚠️ **这是 flag 的属性，不是场景的属性**：用 `--encoder_batch_size 4`（04 与 06 用的值），
bench 是**九次**前馈。garden（4 次）、bicycle（5 次）上**按字面不成立**——
任何视角数超过一个 batch 的场景都无法满足按字面理解的 ②。

**③ 链路里有一次我们完成的训练 —— 成立。**
`epoch_57-step_10000.ckpt`，137 个 `physics_scheme` 张量，
**相对 step 500 的平均相对权重变化 1.82（最大 35.5）**⇒ 头是真训过的，不是近初始化。
decoder 以 `strict=True` 加载（缺参会抛）。链路其余部分全是冻结的官方 IGGT。

**④ 每个 3D 实例都挂着 (E, ν, ρ) —— 成立，bench 9/9**（garden 88/88，bicycle 12/12）。
两条诚实附注：**#5 是 1,025 高斯的碎片，不是物体**——它有数是因为它是个簇，不是因为它有意义；
**#8/#9 是 stuff（墙、地板），已标注。**

### 交付物 3：并排——它实际证明的比「不明显更差」强

**两半逐字节相同：视角 00/12/24 上各 762,048 像素，差异像素 0 个。**
标签与 `ticket03/hdbscan/bench_labels_default.npz` **逐位相同**
（`np.array_equal` True、ARI 1.0000、9 个 id 的计数全等），六张叠加 PNG 与 03 的 `cmp` 相同。
⇒ **挂上物性头、多 trace 32 个通道，对分割的改变恰好是零。**
这块板是**构造上的 null diff**（实例流是同一份计算），已如此标注，没有当成巧合来卖。

### garden + bicycle

**garden —— 在 31.5% 残缺 PLY 上跑通、不崩，全程标注为 partial，从未冒充完整 garden。**
185 视角、1,839,236 高斯、96.25% 被 trace、4 次前馈、**88 个实例**、642 s，每个实例都有 (E,ν,ρ)。
**自控制组**：两次完整跑 → 同样 88 个实例，**ARI 0.9997**，可复现。
⚠️ **但与 03 号票的 garden 对不上（88 vs 82，ARI 0.5638），且两次 trace 不是同一配置。**
`--trace_crop_aligned` 解释了覆盖率差（用默认值重跑精确复现了 03 的 95.3%），
但 crop-aligned 的控制组给的是 79 而非 82 ⇒ **还有第二个 flag 不同，没有定位。**
就此打住——garden 的任务是「不崩」，它不崩。

**bicycle —— 完好数据上跑通、不崩，但分割不好看。**
194 视角（5 次前馈）、6,131,954 高斯、90.2% 被 trace、792 s、**12 个实例**，都有 (E,ν,ρ)。
⚠️ 在出厂的 50/10/0.06 上，**地面、长椅和自行车本身并成一个 2,731,731 高斯的簇（占全场景 44.5%）**，
只有植被和背景分开了。⇒「同一套脚本在第二个真实场景上不倒」在完好数据上做到了，
**但 bicycle 不是分割的成功案例，没有当成成功来展示。**

### ⚠️ 四条与地图冲突的实测

1. ⚠️ **G8 被低估了，而且它咬了 04 和 06。**
   「bench 免疫（36 视角一次前馈）」只有在整个场景走一次前馈时才对。
   **04 与 06 都用 `encoder_batch_size=4` trace 的 bench = 9 个特征空间。**
   实测（同权重、其余全同）：batch-4 vs batch-48 的 `gau_inst_feat`
   maxabs 3.59e-2 / **相对 0.199**（噪声底 6e-7），**标签从 9 个实例变成 11 个，ARI 0.8708**
   （零标签变动的底）。⇒ **免疫属于那个 flag，不属于视角数。**
   （它们的池化口径读数**仍然有效**——那些比较的两侧共享同一个特征空间。）
2. ⚠️ **03 号票的 bench 表把两个高斯数写反了。**
   逐 id 把 mask 渲染回照片核实：**id 4 = 玩具车 13,434；id 6 = 葡萄 7,581。**
   （orchestrator 已用 `bench_labels_default.npz` 独立核实：id4=13,434、id6=7,581，
   05 号票是对的；id↔物体的命名一直对，错的是挂上去的两个数。已回写 03、06 与地图。）
3. ⚠️ **「零标签变动」的噪声底是 bench 的事实，不能外推。**
   bench 上精确成立；garden 的底是 **ARI 0.9997**，小但不是零，
   而 garden 上换一个真正不同的配置会把它甩到 ARI ~0.52。
4. **03 自己的 bench 与 garden 跑用了不同的 `--trace_crop_aligned`**（96.2% vs 95.3% 覆盖率），
   而该 flag 不进 `.pt` 的 provenance——06 因此白跑一趟，05 也差点。

### 没做

- **没有完整的 garden**（盘上仍只有 435 MiB 的截断拷贝，要重取约 1.45 GB）。
- **没定位 garden 与 03 差异的第二个 flag**（覆盖率已由 crop-aligned 解释，79 vs 82 没有）。
- **没有裁定哪个分辨率的物性更对**——bench 没有物性 GT，本图也不承诺量化声明。
- **没有把 04/06 在 `encoder_batch_size=48` 下重跑**（影响已量化，见 1）。
