# 06 — 量「成员集合」这处不等价：GT mask 的高斯 vs HDBSCAN 的高斯

Type: task
Status: closed
Blocked by: [02 — 物性头训练接线与运行点](02-train-physics-head.md) ✅ closed, [04 — trace 两路特征](04-trace-two-feature-sources.md) ✅ closed
Blocks: —
Assignee: subagent (2026-09-09)

> **诊断票，不阻塞交付。** 01 号票判了候选 A，A 不消除训练/推理失配，
> 它把失配挪到**可量的位置**。两处不等价里的第一处（池化口径）在 04 量；
> 第二处（成员集合）要训好的头才能量，落在这里。
> 排在 05 之后做；时间不够就如实标「未量」，**不许用「看起来差不多」代替**。

## Question

`physgm_dpt` 训练时的实例成员是 **GT instance mask 圈的像素**；
推理时是 **HDBSCAN 在高斯上聚出来的高斯集合**。这条差异不可能消除，只能量。

**做法**：在 `3dovs/bench` 上，用**同一份 `gau_phys_feat`、同一个训练好的 MLP**，
只换成员集合，比两组 (E, ν, ρ)：

- **甲**：GT mask 反投影到高斯 → 该集合上 `num_ray` 加权平均 → MLP → denormalize；
- **乙**：HDBSCAN 实例的高斯集合 → 同样的平均 → 同一个 MLP → denormalize。

⚠️ 甲乙的实例**不是一一对应的**（HDBSCAN 会把一个 GT 物体切开、也会把几个并起来）。
先定一条配对规则（建议：高斯集合的 IoU 最大者配对，并报未配上的两侧数量），
配对规则本身要写进 Answer——不同的配对规则会给出不同的差值。

## 一并要报的

- **噪声底**：同一条路跑两遍的差（地图 F4：`atomicAdd`，maxabs 3.4e-3 / 相对 1.7e-6）。
  差值不显著高于噪声底就不算差异。⚠️ 不许用 `torch.equal`。
- **`mu_spread`**（01 号票 (d) 条的定义）：每个高斯单独过一遍同一个 MLP 得到的 `mu` 的标准差。
  甲乙两侧都报——若乙的 `mu_spread` 明显大于甲，说明 HDBSCAN 把不同材质的东西聚进了一个实例，
  这比 (E,ν,ρ) 的均值差更能说明问题。
- **头输出的 `var`** 与 `mu_spread` **分开摆**，别合成一个数。

## 完成判据

一张表：每个配上的实例一行，甲的 (E,ν,ρ)、乙的 (E,ν,ρ)、相对差、两侧的 `var` 与 `mu_spread`；
加上噪声底、配对规则、未配上的实例数。

一句话结论：**成员集合这处不等价，在本图的判据（「有个值就行」）下是可忽略的，还是不可忽略的。**
若不可忽略，写清楚它会怎么影响后续（那是下一张图的事，不是本图的）。

---

## 04 号票交下来的前提（2026-09-09，别再重新发现）

1. **甲（GT mask 侧）的高斯成员集合 04 已经算好了**：把 mask 的 one-hot **加一张常数 1.0
   覆盖平面**穿过同一批相机同一份高斯，`share = occ/coverage`，`argmax` 且 `share > 0.5`。
   1,005,708 个被 trace 的高斯里 512,699 个落到某实例，其余是 GT 留 0 的墙/地板。
   **照抄 `scripts/check_iggt_phys_trace.py`，不要另发明一套反投影。**
2. **bench 的 GT 是 10 个实例**（36 视角并集上 id 1…10），不是老图记的 8 个。
3. **池化就是「把该实例的高斯的 `gau_sem` 加起来」**。分母（`Σ num_ray` / `Σ blend_mass`
   / 任何正标量）因 decoder 首层 LayerNorm 的尺度不变性**完全不影响 MLP 的输入**。
   别在分母上花时间。
4. **预期主因是实例尺寸，不是加权**：04 实测 2D↔3D cos 在 >1000 高斯的实例上 0.95–0.99，
   在 534–1306 高斯的四个小/薄实例上塌到 0.39–0.82；加权 vs 等权 cos = 0.996。
   ⇒ 本票的表**必须带 `n_gau` 列并按它排序**，否则会把尺寸效应误读成成员集合效应。
5. **噪声底 ≈ 6e-7 相对**（04 的控制组读数）。
6. ⚠️ `mu_spread` 逐高斯 decode 时，喂 MLP 的向量比训练时短约 50×、逐高斯尺度散布 100×，
   **只因为 LayerNorm 尺度不变才有意义**。若发现 `mu_spread` 反常，先怀疑这条。
7. 现成资产：`.pt` 在
   `/mnt/storage_pool/liaoyuanjun/runs/ticket04_iggt_phys/bench/gaussian_iggt_phys_feat.pt`；
   harness 在 `scripts/check_iggt_phys_trace.py`（docstring 里有两条命令，约 80 s 复现）。

---

## ⚠️ 03 号票推翻了本票的前提（2026-09-09），方法要改

本票原来的甲侧是「GT mask 反投影到高斯」。**03 号票实测：`sam/mask` 的 id 不是跨视角一致的**
——帧 00 是 id 1–8、帧 12 是 1–7、帧 24 是 1–9，**同一个 id 在不同帧指不同物体**。
它是逐帧 SAM 输出，**根本不是 3D 实例 GT**。

⇒ **04 号票报的「bench GT 是 10 个实例」也不成立**（那是 36 帧上 id 的并集最大值，
与老图记的「8」是同一类错误——那是帧 00 的计数）。
⇒ **04 的池化口径读数仍然有效**：它两侧用的是同一套 id 语义，比的是「2D 池 vs 3D 池」
这个协议差，污染两侧对称抵消。**但它不能被当成实例级的物性结论。**

### 改后的做法

**第一步：先造一套真正的 3D GT 实例，或者证明造不出来。**
逐帧、逐 id 单独 trace 到高斯（04 的 one-hot + 常数覆盖平面那套现成），
得到每个 (帧, id) 的高斯集合，再把**高斯集合重叠度高的跨帧 id 连成同一个 3D 实例**
（连通分量，阈值写进 Answer）。这本来就是 trace 白送的能力。

**若连接结果是歧义的**（一个 3D 实例被链到互相矛盾的帧 id、或链出的实例数不稳定），
**那就是本票的答案**：`3dovs/bench` 上不存在可用的 3D 实例 GT。
这个结论本身很有价值——它一次性判掉一整类未来的指标（任何拿 `sam/mask` 当 3D GT 的 IoU / mIoU），
**如实写出来即可，不要硬凑一个数**。

**第二步（只有第一步成功才做）**：用连好的 3D GT 实例集合作为甲，HDBSCAN 实例作为乙，
按原方案比两组 (E, ν, ρ)。配对规则仍要写进 Answer。

### 03 号票交下来的其它前提

- **噪声底是「零标签变动」**：两次独立 trace 得到的 9 个实例完全相同（ARI 1.0000）。
  所以本票任何标签差异都是真的。
- **bench 的 HDBSCAN 实例是 9 个**：2 个 stuff（墙 555,810 高斯、地板 366,893）+
  6 个真物体（猫 27,402 / 娃娃 18,876 / 葡萄 13,434 / 叶 7,840 / 车 7,581 / 蛋挞 6,847）+
  1 个 1,025 高斯的碎片。**乙侧不用重跑聚类**，直接用 03 的标签。
- **bench GT 是 84.2% stuff 像素**（不是地图写的 62%）⇒ 甲侧能配上的物体实例本来就少。

---

## Answer（2026-09-09 05:10 关闭）——结局是 (a)：**GT 造得出来**，且失配可忽略

### 第一步推翻了 G9 的**结论**（观察是对的）

G9 的**观察**逐条复现：帧 00 是 id 1–8、帧 03 是 1–7、帧 21 是 1–10，同一 id 在不同帧指不同物体。
但 G9 的**结论**「没有可用的 3D GT」**太强了**。

逐 `(frame, id)` 单独反投影（照抄 04 的 one-hot + 常数 1.0 覆盖平面，`share = occ/coverage`,
`argmax`, `share > 0.5`），再按跨帧高斯集合重叠连通：

- 285 个 `(frame, id)` 节点 → 按 co-visible 受限 IoU 连通分量。
- **恰好 7 个分量在 36 帧里每帧各占一个 id，零矛盾**（没有任何分量在同一帧里占两个 id），
  **且这 7 个在 τ ∈ [0.3, 0.7] 上逐位相同**。
  （我复核过 `full_span_components`：τ=0.05/0.1→4、0.2→6、**0.3/0.4/0.5/0.6/0.7→7**、0.8→4、0.9→2，
  平台是真的；并且逐字节比过三份 JSON，**唯一的差异就是里面记录的 `tau` 字段本身**。）
- 这 7 个划走 **1,003,566 / 1,005,708 = 99.79%** 的被 trace 高斯（我复核过求和）。
  33 个 `(frame,id)` 节点（约 2,142 高斯，只在部分帧可见的碎片）连不成帧一致，剔除。

⇒ **bench 有 7 个帧一致的 3D GT 实例（2 stuff + 5 物体）。**
「8」（帧 00 计数）和「10」（36 帧 id 并集最大值）都错，G9 说对了这一半；
但「没有 3D GT」也错。**G9 已按此改写。**

### 第二步——表（head = `epoch_57-step_10000.ckpt`，`global_step=10000` 从 ckpt 断言）

ρ kg/m³、E Pa。`d_x` = 相对差。

| A | B | IoU | n_A | ρ_A | ρ_B | d_ρ | E_A | E_B | **d_E** | ν_A | ν_B | d_ν |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 7 墙 | 8 | 0.969 | 540,783 | 1932.7 | 1971.9 | 0.020 | 2.293e10 | 2.362e10 | 0.030 | .2604 | .2604 | 0.0001 |
| 6 地板 | 9 | 0.949 | 382,767 | 2408.8 | 2393.2 | 0.007 | 2.371e10 | 2.328e10 | 0.018 | .2696 | .2699 | 0.0012 |
| 4 猫 | 1 | 0.978 | 27,605 | 120.1 | 120.8 | 0.006 | 2.528e6 | 2.530e6 | **0.0006** | .4426 | .4424 | 0.0003 |
| 2 娃娃 | 3 | 0.895 | 17,374 | 864.5 | 883.9 | 0.022 | 6.588e8 | 6.649e8 | 0.009 | .3528 | .3520 | 0.0022 |
| 1 车+叶 | 6 | **0.452** | 15,011 | 1334.4 | 1290.4 | 0.034 | 1.891e9 | 2.725e9 | **0.362** | .3137 | .3058 | 0.026 |
| 3 葡萄 | 4 | 0.929 | 13,384 | 2049.9 | 2224.8 | 0.082 | 1.448e10 | 2.064e10 | **0.351** | .2783 | .2717 | 0.024 |
| 5 蛋挞 | 2 | 0.921 | 6,642 | 1971.2 | 2193.5 | 0.107 | 8.729e9 | 1.415e10 | **0.474** | .2851 | .2782 | 0.024 |

**model space 里**（z 单位）：`|mu_A − mu_B|` 最大 **0.16 z**，
除以该实例自己的 `mu_spread` 最大 **0.28**。

### 规则、噪声底、未配上的

- **连接规则**：跨帧高斯集合 IoU（限制在 co-visible 高斯上）的连通分量，τ = 0.5，
  只保留跨满 36 帧的分量。**敏感度：7 个实例集合与整张物性表在 τ = 0.3 / 0.5 / 0.7 上逐位相同。**
- **甲侧划分规则**：一个高斯归属于「在该分量可见的那些帧里，以严格多数认领它」的分量。
  ⚠️ **按 co-visibility 归一是承重的**——按原始帧数归一会把墙从 541k 错误地缩到 274k。
- **配对规则**：每个甲取 IoU 最大的乙（允许多对一）。**甲 7/7 全配上；乙 9 个里 2 个没配上**
  ——B5（1,025 高斯的碎片）和 B7（「叶子」7,840），**两个都是甲实例 1 的碎片**。
- **配对规则敏感度**：换成「甲吸收所有 ≥20% 落在它里面的乙」，**只有 A1 行变，且变得更差**
  （d_E 0.362 → 0.467）。其余行不动。⇒ 配对规则只在「HDBSCAN 把一个 GT 物体切开」处才有影响。
- **噪声底**（同成员集合、同 trace 命令跑两遍，端到端到 SI）：
  **ρ 5.7e-07 / E 2.2e-06 / ν 1.1e-07**；特征级 `gau_phys_feat` 相对 1.48e-07。
  表里每个差都高出噪声底 5–6 个数量级，**全是真的**。

### 唯一一处定性失败

甲实例 1 是**一个** GT 物体，被 HDBSCAN 切成**三块**：B6(46.9%) / B7(43.8%) / B5(6.8%)，
`IoU(A1, B6∪B7) = 0.809`，覆盖 A1 的 90.7%。乙侧那 2 个未配上的全在这里。
⚠️ 值得注意的方向：**A1 合并侧的 `mu_spread` 比 B6 单独还低**（.678/.573/.611 vs .846/.737/.769）
⇒ **没有证据表明 HDBSCAN 把不同材质并进了一个实例；在 bench 上它恰恰相反，是过切分。**

### 一句话结论

**在本图的判据（「有个值就行」）下，成员集合这处不等价可忽略。**
两种成员集合让 `mu` 移动从不超过 **0.16 z**，即该实例自身 `mu_spread` 的 **≤0.28 倍**，
且远在头自报的 σ（√var ≈ 0.65–1.4）之内；没有任何实例改变材料档次，ν 从不超过 2.6%。

⚠️ **但若有人把 SI 数字当字面值读，它就不可忽略**：因为 E 是经 `10^(1.318·z)` 解码的，
同样 ≤0.16 z 在 7 个实例里的 3 个上变成 **Pa 上最多 +47%**（ρ 最多 +11%）。
⇒ **任何把 E 报到优于约 1.5 倍精度、或跨场景/跨实例比较 E 的主张，都不被支持**
——光是成员集合就能挪那么多。这是下一张图的问题。

### ⚠️ 与既有记录不符、要回写的五条

1. **G9 的结论要软化**（已改）：`sam/mask` **按原样**不是 3D GT，
   但经逐 `(frame,id)` 反投影 + 重叠连接**可以变成**一个。
   这**不**拯救在原始 id 上直接算 IoU/mIoU，但确实拯救了「bench 有 3D GT」这个概念。
2. ⚠️ **分歧的「大小」预测不了物性差**。娃娃有 10.5% 成员不同（占乙池化权重 11.6%）→ d_E 0.009；
   葡萄只有 7.1% 不同（8.9%）→ d_E 0.351。`n_gau`、IoU、池化权重占比**都排不出 d_E 这一列**。
   ⇒ **这不是 04 号票预测的实例尺寸效应**（猫 27k → 0.0006，而葡萄 13k → 0.351、娃娃 17k → 0.009）；
   **是「哪些高斯不同」，不是「多少个不同」。**
3. ⚠️ **04 号票的 `.pt` 是 step 500 traced 的，必须重 trace。** `gau_phys_feat` 是物性头的函数。
   本票在 step 10000 重 trace；`num_ray` 与 04 的逐位相同、`gau_inst_feat` 相差 3.4e-07
   ⇒ 几何可证相同，**03 号票的 HDBSCAN 标签可直接复用**。
   ⚠️ **复现 04 的几何需要 `--no_trace_crop_aligned`**（默认 `True` 给的是 95.9% 覆盖率，不是 96.22%），
   这个 flag **没有记进 `.pt` 的 provenance**，害本票白跑一趟。**已回写地图。**
4. ⚠️ **32 维物性特征在训练中长大了约 1060×**（|x|max 0.485 @step500 → 515.2 @step10000）。
   **只因为 decoder 的 LayerNorm 才无害**——正是 01 号票更正块认定的那条承重事实。
5. **Sanity 旗标，不是主张**（本图不承诺物性质量）：step-10000 的头把墙/地板判成 E ≈ 2.3e10 Pa、
   **葡萄 1.4e10 Pa**、毛绒猫 2.5e6 Pa。**软猫合理，硬葡萄不合理。**

### 文件

- **新增**（仓库内）：`scripts/check_iggt_phys_membership.py`——两阶段 + 两个 sweep 的完整 harness，
  docstring 里有两条复现命令。**没有修改任何已跟踪文件。**
- **产物**（`/mnt/storage_pool/liaoyuanjun/runs/ticket06_membership/`，599 MB）：
  `bench_step10000/`（+ `_run2` 控制组）、`perframe_backproj.pt`（118 MB 缓存，重跑约 90 s）、
  `report_final/{run.txt,membership_table.json}`、`report_tau{0.3,0.5,0.7}/`、
  `iou_{covisible,global}.npy`（285×285）、`link_sweep.json`。

### 没做

- **只有 bench。** garden 没有 `sam/mask`，甲侧在那儿根本造不出来。
- **没扫 `share > 0.5` 与 `min_ray`**，两者原样继承自 04 的反投影
  ——重调会让与 04 的比较失去可比性。
